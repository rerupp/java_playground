# Spring Boot creates a JPA EntityManagerFactory using Hibernate so override some defaults
spring.jpa.hibernate.ddl-auto: disable
spring.jpa.hibernate.naming_strategy: org.hibernate.cfg.ImprovedNamingStrategy
spring.jpa.database: H2
spring.jpa.show-sql: true